name: chinook-postgres-pulumi
runtime:
  name: python
  options:
    toolchain: pip
    virtualenv: venv
description: Example chinook api
config:
  pulumi:tags:
    value:
      pulumi:template: aws-python
